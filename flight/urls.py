from django.urls import path

# Legacy URLs - being replaced with REST API in api_urls.py
app_name = 'flight'

urlpatterns = [
    # API is now at /api/v1/ (see spacetravel/urls.py and flight/api_urls.py)
]
